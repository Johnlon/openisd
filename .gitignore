node_modules/
dist/
test-results/
test-logs/
ci-logs/
build/
.server.pid
.server.log
.server-*.pid
.server-*.log
dq_report.txt

# Scraper cache dirs — all start with _ and are never committed
drivers/**/_*/

# Federated collections symlinked in locally from a sibling winisd_drivers
# checkout (scripts/link-driver-repo.sh) — dev convenience only, never tracked.
drivers/dayton-audio
drivers/loudspeakerdatabase
drivers/parts-express
drivers/sb-acoustics
drivers/scan-speak
drivers/soundimports
drivers/wavecor

# Generated at build time by scripts/bundle-drivers.mjs — NEVER track it (human ruling
# 2026-07-21: CI uses BOTH repos and regenerates; a derived bundle is not checked in).
# (The old pattern `src/drivers-bundle.json` was root-anchored and matched nothing,
# so the file stayed tracked until 2026-07-21.)
packages/ui/src/drivers-bundle.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Tool caches
.playwright-mcp/

# Vitest coverage output
coverage/
