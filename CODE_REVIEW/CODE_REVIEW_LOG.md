# CODE REVIEW LOG

Code-review runs (append-only, one timestamped section per run — written by the code-reviewer subagent). The curated findings doc is CODE_REVIEW.md (stable §N IDs); do not confuse the two.

<!-- new sections appended below -->

## 2026-07-19T11:27:41Z — code review of 431a13d8b^..431a13d8b
- [SEVERITY: high] packages/ui/src/shells/original/OriginalShell.vue:47-53 — `selectedBox`/`state.box` sync is one-directional once an unsupported box type (bandpass6/abc) is selected, causing a silent desync
  why it surprises/costs a reader: the second watcher (`watch(() => state.box, ...)`) only syncs `selectedBox` back from an external `state.box` change when `SUPPORTED_BOX.has(selectedBox.value)` is true — i.e. exactly when the two values already agree and the sync is a no-op. When the user has `selectedBox` on `abc`/`bandpass6` (pending state, `SUPPORTED_BOX.has(selectedBox.value)` is false) and `state.box` is then changed by something outside this component (loading a saved design via `useDesignIO.ts:50` or `App.vue:31`, both of which do `state.box = o.box`), the guard blocks the update: `selectedBox` stays stuck on `abc`/`bandpass6`, the Box tab keeps showing the ABC diagram and the "response model pending" banner, but the store's actual `state.box` (and the live curves computed from it via `store.ts:158`'s `watch([driver, syncedP, () => state.box], _scheduleSweep)`) has silently changed underneath the visible UI. The user edits `Vb`/`Vf` believing they're shaping an unmodelled ABC box while they are actually driving a real, different enclosure type with no visible indication.
  suggested fix: drop the guard condition (or invert it) so any external `state.box` change always re-syncs `selectedBox`, e.g. `watch(() => state.box, (b) => { selectedBox.value = b; })` — the guard should protect against overwriting a user's in-progress pending selection with the value watcher 1 itself just wrote (same value, no-op already), not against real external changes.
- [SEVERITY: medium] packages/ui/src/shells/original/OriginalShell.vue:90 — `driveV` (`Math.sqrt((state.P.Pin ?? 1) * (driver.value?.Re || 8))`) is now a third verbatim copy of the same formula
  why it surprises/costs a reader: identical logic (including the same magic fallback literals `1` W and `8` Ω) already exists in `packages/ui/src/shells/classic/ClassicShell.vue:42` and `packages/ui/src/components/SignalPanel.vue:8`. A third hand-copy means a future change to the fallback/units convention (e.g. if the `8` Ω default is ever revisited) has to be hunted down and fixed in three places, and any two of the three drifting apart would be a subtle, hard-to-spot behavioural difference between skins.
  suggested fix: extract a shared `driveVoltage(state.P.Pin, driver.value?.Re)` helper (e.g. in `store.ts` or a small computed util) and have all three call sites use it.
- [SEVERITY: low] packages/ui/src/shells/original/OriginalShell.vue:69 — rear-chamber resonance for the sealed box is labelled `Fsc`, while the equivalent value in `BoxPanel.vue` (`packages/ui/src/components/BoxPanel.vue:26,101`) is labelled `Fc`/`Fh` depending on box type, with no `Fsc` anywhere else in the codebase
  why it surprises/costs a reader: a reader jumping between the Classic/Original skins for the same physical quantity sees two different labels (`Fc` vs `Fsc`) with no comment explaining the WinISD-specific naming difference, making it look like a typo or a different quantity rather than a deliberate per-skin label choice.
  suggested fix: either reuse the same label as `BoxPanel.vue` or add a one-line comment noting why the Original skin's label differs (if it's the literal WinISD 0.7.0.950 label being ported).
- [SEVERITY: low] packages/ui/src/shells/original/OriginalShell.vue:51 — `watch(selectedBox, ..., { immediate: true })` runs once at setup purely to reassign `state.box = selectedBox.value` from `state.box` itself
  why it surprises/costs a reader: `selectedBox` is initialised from `state.box` (`ref<OgBox>(state.box)`), so the immediate call is a self-assignment (`state.box = state.box`) — harmless, but a reader has to trace both lines to confirm it isn't doing something on mount.
  suggested fix: drop `{ immediate: true }` (the initial value is already correct) or add a short comment stating it's a deliberate no-op guard for symmetry.

No findings against `packages/ui/src/skins.ts`, `packages/ui/src/App.vue`, `packages/ui/src/types.ts`, `packages/ui/test/skins.test.ts`, or `packages/ui/test/original-skin.browser.spec.ts` — the skin-seam wiring is a clean, minimal extension of the existing `classic`/`modern` pattern, and both test files assert meaningful, specific behaviour (box-type count, pending-state banner presence/absence, chart re-render, reload persistence) rather than opaque values. `npx eslint` on all six changed/added files is clean.

## 2026-07-19T14:19:14Z — code review of 8147d3033 (Original skin wholesale port)
- [SEVERITY: high] packages/ui/src/shells/original/OriginalShell.vue:91 — `Leff = state.P.ventL + 0.732 * state.P.ventD` hardcodes the vent end-correction as a bare literal instead of importing `END_CORRECTION` from `@openisd/engine`, even though the file already imports `RHO, C` from the same module and `END_CORRECTION` is exported from `packages/engine/src/constants.ts:20` with its derivation comment.
  why it surprises/costs a reader: a second, uncommented copy of a named physics constant is exactly the "forked physics" / magic-number trap the house rules exist to prevent — a future change to the end-correction model in `constants.ts` silently stops applying to this file's vent-Fb readout.
  suggested fix: `import { END_CORRECTION } from '@openisd/engine'` and use `state.P.ventL + END_CORRECTION * state.P.ventD`.

- [SEVERITY: medium] packages/ui/src/shells/original/OriginalShell.vue:96-109,223,231 — `prVas`/`prFs`/`prFsMass`/`prQms`, `driveV`, and `advSoundVelocity` are verbatim copy-pasted formulas (including comment wording) from `packages/ui/src/shells/classic/ClassicShell.vue:31-49` and `packages/ui/src/components/PRPanel.vue:17-30`/`PREditModal.vue:18-27` — this commit adds a 5th independent copy of the PR T/S math and a 3rd copy of `driveV`/sound-velocity.
  why it surprises/costs a reader: none of these live in the engine layer even though they are pure physics derivations from T/S params (Fs=1/(2π√(Mmd·Cms)) etc.); a fix or unit correction now has to be hunted down and applied identically in 3-5 files, and they will drift silently since nothing enforces they match.
  suggested fix: extract `prVas`/`prFs`/`prFsMass`/`prQms`/`driveV`/`soundVelocity(T)` as named functions in `packages/engine/src` (arrows point up rule) and have all shells/components call the one implementation.

- [SEVERITY: low] packages/ui/src/shells/original/OriginalShell.vue:344-345 — `v-for="(d, i) in state.compare" :key="i"` keys the compare-list checkbox rows on array index (same pre-existing pattern copied from ClassicShell:207), a known Vue reactivity trap: removing a non-last row reuses/misassigns DOM nodes for the shifted rows instead of moving them.
  why it surprises/costs a reader: with more than one compare overlay, unticking an earlier row can leave stale checked/hover state on the wrong row until Vue's patch happens to correct it — subtle, hard to reproduce visually.
  suggested fix: key on a stable identity (e.g. `d.name` if unique, or a generated id stored on the compare entry) instead of the loop index.

- [SEVERITY: low] packages/ui/src/shells/original/OriginalShell.vue:246-259 — the `unit()`/`cycleUnit()` decorative unit-label cycler changes only the displayed unit text, never the underlying value's scale (documented in the comment as intentional mock parity), so clicking "l" → "cuft" on the Volume field leaves the numeric value unconverted under a now-wrong unit label.
  why it surprises/costs a reader: even though it's commented as deliberate fidelity to the mock, a real user (not just a reviewer) can trigger a display that looks like a unit conversion but silently is not one — worth a one-line UI hint (e.g. disable/grey until real conversion exists) rather than only a code comment, so the risk is visible to users, not just to code readers.
  suggested fix: either wire real conversion or visually mark these unit labels as non-functional (e.g. a tooltip "display label only") so users aren't misled.

No findings on: Vue reactivity of the `selectedBox` ↔ `state.box` two-way sync (watchers correctly guard against ping-pong, each only writes on real divergence); `onMounted`/`onUnmounted` pairing for the document click-outside listener and the tone generator (`toneGenerator.stop()` called on unmount); lint (`npx eslint`) and typecheck (`npx vue-tsc --noEmit -p packages/ui`) both clean on the changed files; no `eslint-disable`, `console.*`, or `_foo` lint-dodge renames introduced; the added browser tests assert meaningful, scoped DOM state (e.g. scoping "response model pending" assertions to `.tab-section.active` to avoid false negatives from hidden `v-show` tabs) rather than opaque values.

## 2026-07-19T22:37:49Z — code review of e097f5e19 (Original skin: color cycle, bandpass Frc/Tuning readouts, 1st-port-resonance label)

- [SEVERITY: medium] packages/ui/test/original-skin.browser.spec.ts:109-115 — the new "bandpass Box tab shows calculated Frc + Tuning-freq readouts" test only asserts the *label* text ("Frc", "Tuning freq") is present, never the calculated *value*.
  why it surprises/costs a reader: the commit's whole claim is "real computed values via the same closed form the engine's circuit uses, not literals" — this test would still pass if `rearResonance`/`frontTuning` were reverted to a hardcoded literal (or broken to always return null/"—"), so it doesn't guard the behaviour it's named for and gives false TDD confidence.
  suggested fix: assert the input's numeric `.value` (e.g. via `.calculated.greyed` locator's value) is a finite, non-"—" number, and ideally cross-check it against the expected closed-form result for a known driver/Vb/Vf fixture.

- [SEVERITY: low] packages/ui/src/shells/original/OriginalShell.vue:42 vs mock/script.js:1221 — `TRACE_PALETTE` invents a new 6-colour set (`'#c9c92e','#2f6db5','#c0392b','#27ae60','#8e44ad','#e67e22'`) that only shares its first entry with the mock's own established `colorPalette` (`'#c9c92e','#e34b4b','#3a7bd5','#2e8b57','#c23bc2','#2ec9c9','#e08a2e'`, mock/script.js:1221), despite the shell's stated goal (file header, OriginalShell.vue:1-15) of being "a faithful wholesale port of the mock".
  why it surprises/costs a reader: this is a second, silently-diverging source of truth for something the mock already fully specifies (palette + cycle behaviour) — a maintainer skimming the mock for the "true" palette will get a different list than what actually renders, and any future palette edit now has to remember to touch two places that look unrelated.
  suggested fix: either literally reuse the mock's `colorPalette` values, or add a one-line comment explaining why this skin intentionally departs from the mock's palette.

- [SEVERITY: low] packages/ui/test/original-skin.browser.spec.ts:101-107 — the color-cycle test only checks that the colour changes after one click; it never exercises the wrap-around boundary (`(traceIdx.value + 1) % TRACE_PALETTE.length`).
  why it surprises/costs a reader: an off-by-one in the modulo (e.g. cycling past the last colour to `undefined`/`transparent`) would not be caught since the test never clicks `TRACE_PALETTE.length` times.
  suggested fix: click the button `TRACE_PALETTE.length` times and assert the colour returns to the initial value.

No high-severity findings. The `.value`/computed refactor (`WINISD_TRACE`) is correct in both script (`.value` used consistently in `currentDesign`/`cursorVal`) and template (auto-unwrapped in `:style`) — verified by reading all six use sites. `helmholtzFb(volume)` correctly factors out the pre-existing `ventFb` closed form for reuse by `frontTuning`; reactivity is intact since `state.P.ventL/ventD`/`ventArea.value` reads inside the plain function are tracked by the enclosing computed's synchronous execution. `rearResonance` (Fc formula) and `helmholtzFb` (Helmholtz-resonator formula) are distinct physics, not accidental duplicates of each other. Pre-existing UI-side reimplementation of the engine's port-tuning closed form (packages/engine/src/circuit.ts:85-103) versus this shell's own `helmholtzFb` predates this commit and is an architecture-layer concern — noted briefly, deferred to arch-reviewer. `npm run lint` and `vue-tsc --noEmit` are clean on the touched files (one pre-existing, unrelated Playwright `.skip()` warning in a different file).

## 2026-07-20 01:54:50 UTC — code review of 6bdc5862e..3a1c25fa9 (OgFilters.vue, OgTune.vue, OriginalShell wiring, original-skin.browser.spec.ts)

- [SEVERITY: high] packages/ui/src/shells/original/OgTune.vue:35-38 — typing a T/S value character-by-character breaks after the first keystroke (reproduced live: typing "1","2","3" into the Fs field leaves the input showing "1.00", not "123")
  why it surprises/costs a reader: every keystroke calls `onField` → `enterDriverField` → reactive `driverRaw` change → the bound `:value="disp(...)"` recomputes and Vue overwrites the input mid-typing, eating subsequent digits/decimal point — the panel is effectively unusable for entering any multi-digit or decimal value by typing (only paste/`.fill()` works, which is why the browser test using `.fill()` + `dispatchEvent('input')` didn't catch it)
  suggested fix: buffer the raw typed string like the sibling `DriverWhatIfPanel.vue` does (`rawVals`/`rawOrFmt`, cleared on blur) so the formatted `disp()` value doesn't clobber the field while focused/being typed into

- [SEVERITY: medium] packages/ui/src/shells/original/OgFilters.vue:26-31 — `DEFAULTS` is a byte-for-byte duplicate of `defaults` in `packages/ui/src/components/FiltersPanel.vue:5-9`, but the comment on line 26 claims "one source of truth for the shape"
  why it surprises/costs a reader: the comment tells the next reader these are shared/single-sourced when they are actually two independently-maintained literal copies; a future change to one default (e.g. peaking Q or gain) will silently drift from the other skin with no compiler/test signal
  suggested fix: either export one `FILTER_DEFAULTS` const from a shared, framework-free module (e.g. alongside `FilterType` in the engine or a small shared UI util) and import it in both panels, or reword the comment to state plainly that it's an intentionally-duplicated literal and why

- [SEVERITY: medium] packages/ui/src/shells/original/OgTune.vue:41-42 — `snapshot` is set twice: once at declaration (`let snapshot: DriverRaw = { ...driverRaw.value }`) and again, redundantly, in `onMounted`
  why it surprises/costs a reader: given the component is only ever created via `v-if="state.editDriver"` in OriginalShell.vue (destroy/recreate each toggle, confirmed by the diff), `setup()`'s top-level assignment and `onMounted`'s reassignment run in the same synchronous mount pass and always produce the same value — a reader has to work out there's no meaningful difference and will wonder if the duplication is masking a subtlety that isn't there. It's also a *weaker* mechanism than the sibling `DriverWhatIfPanel.vue`'s `watch(() => state.editDriver, ..., { immediate: true })` (packages/ui/src/components/DriverWhatIfPanel.vue:37-38): if OgTune's `v-if` is ever changed to `v-show` (keep-alive-style toggling, a plausible follow-up for a docked panel), `onMounted` fires only once ever and Cancel will silently revert to the very first open instead of the current session's open — the shared panel's `watch(..., {immediate:true})` pattern was written specifically to survive that change; OgTune re-implemented the concept without that resilience
  suggested fix: drop the top-level initializer and use the same `watch(() => state.editDriver, ..., { immediate: true })` pattern as `DriverWhatIfPanel.vue` for consistency and resilience to a future mount-strategy change

- [SEVERITY: low] packages/ui/src/shells/original/OgFilters.vue:23 — `BADGE` record duplicates the `badge` field already present per-entry in `QUICK_ADD` (both map `FilterType` → `'HP'|'LP'|'LT'|'PEQ'`)
  why it surprises/costs a reader: two literal maps of the same information invite drift (e.g. renaming a badge label in one and not the other)
  suggested fix: derive `BADGE` from `QUICK_ADD` (`Object.fromEntries(QUICK_ADD.map(q => [q.type, q.badge]))`) or drop one of the two representations

No findings on: lint (`npx eslint` on both files — 0 errors/warnings), no `any`/casts/eslint-disable, filter-row precision/rounding (`fnum`/`num` display-only, doesn't feed back into a live reactive `:value` the way OgTune's `disp` does, so OgFilters' `v-model.number` inputs don't hit the same typing trap), and the two new browser tests assert real, named behaviour (filter count/type/store state; live Fs preview value and its revert on Cancel) rather than opaque values.

## 2026-07-20 21:25:13 UTC — code review of 9966de8e2^..HEAD (engine formulas + fieldRegistry data model + NumInput dp fix + decimal-place wiring)

- [SEVERITY: high] packages/ui/src/fields/fieldRegistry.ts:102-112, packages/ui/src/shells/original/OriginalShell.vue:620,623,664 — registry entries marked `modeled: false` are actually rendered, and one is semantically wrong
  why it surprises/costs a reader: `ventCrossArea` and `portResonance` are documented as "not yet surfaced" (fieldRegistry.ts:106,111) — a reader trusting the registry as SSOT would believe these aren't shown — yet this same PR wires `fieldDp('ventCrossArea')`/`fieldDp('portResonance')` into live readonly fields. Worse, `portResonance`'s own description says "First pipe/organ resonance of the vent" (a distinct physical quantity from box tuning), but the code binds its dp to `ventFb`, which is `helmholtzFb(state.P.Vb)` — the Helmholtz box tuning frequency (registry already has a correct field for that: `Fb`, "Fh (tuning frequency)"). The registry — billed at the top of the file as "the single, canonical data model... the SSOT that other layers refer back to" — is provably wrong about both its own `modeled` flag and the physical meaning of a field it exports.
  suggested fix: either mark these fields `modeled: true` with a formula matching what's actually displayed, or point OriginalShell's dp lookups at the correct existing field (`fieldDp('Fb')`) instead of borrowing `portResonance`/`ventCrossArea` purely for their dp value.

- [SEVERITY: medium] packages/ui/src/fields/fieldRegistry.ts:233-238, packages/ui/src/shells/original/OriginalShell.vue:733 — `advAirDensity`'s declared formula/dependsOn is fiction for its only real usage
  why it surprises/costs a reader: the registry states `formula: 'ρ = f(T, RH, P)'`, `dependsOn: ['advTemp','advHumidity','advPressure']` implying a live derivation, but the only place `fieldDp('advAirDensity')` is used just formats the fixed engine constant `RHO` (`RHO.toFixed(fieldDp('advAirDensity'))`) — completely independent of the three inputs it claims to depend on. A reader (or a future DQ check) trusting `dependsOn` would wrongly conclude changing temperature/humidity/pressure updates this readout.
  suggested fix: either implement the real ideal-gas density formula so the code matches the registry's claim, or mark the field `provenance: 'entered'`/static and drop the formula/dependsOn until it's real.

- [SEVERITY: medium] packages/engine/test/formulas.test.ts:1-45 — "independently computed" test oracle isn't independent
  why it surprises/costs a reader: the file's docstring claims "the expected values are computed independently here so the extraction is proven behaviour-preserving," but every PR assertion re-derives the SAME expression as the implementation (e.g. `prFs(Mmd, Cms) - 1 / (2 * Math.PI * Math.sqrt(Mmd * Cms))`) rather than a precomputed literal. A shared bug (wrong constant, missing ×1000, sign error) in both implementation and "oracle" would still pass — only `driveVoltage`'s tests use real independent literals (`driveVoltage(100,4) === 20`).
  suggested fix: add at least one hardcoded-literal anchor assertion per PR formula (as already done for driveVoltage/soundVelocity), not just algebraic restatements.

- [SEVERITY: medium] packages/ui/src/components/DriverEditorModal.vue:114,131 — wiring reduced displayed precision for Vas/Sd with no regression test covering the change
  why it surprises/costs a reader: `Vas` went from a hardcoded `:precision="3"` to `fieldDp('Vas')` = 2, and `Sd` from `:precision="4"` to `fieldDp('Sd')` = 1 — a real, user-visible narrowing of decimal places in the driver-editor screen. Per CLAUDE.md's TDD rule, a change to production behaviour should arrive with a test that would have failed before it; only BoxPanel's `ventD` dp change got that treatment (app.browser.spec.ts:40-47) — the DriverEditorModal changes are unverified by any test.
  suggested fix: add a browser test asserting DriverEditorModal's Vas/Sd fields now show 2dp/1dp respectively, matching the pattern used for ventD.

- [SEVERITY: low] packages/ui/src/components/NumInput.vue:51-77 — mouse-click on the native spin buttons isn't tracked by the typing/step distinction
  why it surprises/costs a reader: `onKeydown`/`onWheel` are the only ways `typing.value` is reset to `false` for a "step" (arrow-key/wheel) interaction; clicking the browser's native up/down spinner buttons with the mouse fires only an `input` event, no `keydown`/`wheel`. If a user is mid-typing (`typing===true`) and then clicks a spinner arrow without blurring, `onInput` takes the raw-echo branch instead of the reformat branch, so the display can briefly show an unformatted/compounding float again — the exact defect this fix targets. The model value stays correct in this path; only the display can desync. Not covered by existing spinner tests, which all drive via `ArrowUp`/`ArrowDown` keys (original-skin.browser.spec.ts:308-322).
  suggested fix: also treat a native spinner mouse-click as a step (e.g. detect it isn't achievable directly, so instead reformat unconditionally whenever the delta matches a step-sized change, or reset `typing` on `onFocus` per-click via a `pointerdown` handler on the input).

- [SEVERITY: low] packages/ui/src/fields/fieldRegistry.ts:373 — `filterOrder` given `precision: 3`
  why it surprises/costs a reader: filter order is conventionally a small integer (1st/2nd/3rd/4th order); `precision:3` (e.g. "2.000") looks like a copy-paste from the neighbouring `filterQ`/`filterGain` entries rather than a deliberate choice. Currently inert (no `fieldDp('filterOrder')` call exists yet), so it will mislead whoever wires it up next.
  suggested fix: set `precision: 0` for `filterOrder`.
