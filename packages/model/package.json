{
  "name": "@openisd/model",
  "version": "0.1.0",
  "type": "module",
  "description": "The OpenISD driver record and OpenISDDriver — the app's driver model, its provenance and its derivation. No file format, no DOM, no app state.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@openisd/engine": "*",
    "yaml": "^2.9.0"
  }
}
