{
  "name": "@openisd/persistence",
  "version": "0.1.0",
  "type": "module",
  "description": "Data-access tier: repos over the driver/PR/prefs collections and the storage services they're built on. Takes a storage collaborator, returns domain records. No app state, no DOM beyond the File/localStorage APIs it wraps.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@openisd/design": "*"
  }
}
